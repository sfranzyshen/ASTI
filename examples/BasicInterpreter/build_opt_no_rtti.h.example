/**
 * build_opt_no_rtti.h.example - RTTI-Free Mode (Size Optimization)
 *
 * v21.1.0: Opt-in configuration for size-constrained deployments.
 *
 * ⚙️ THIS IS EXPLICIT OPT-IN - Overwrites Default
 * =================================================
 * To enable RTTI-free mode:
 *
 * cd examples/BasicInterpreter
 * cp build_opt_no_rtti.h.example build_opt.h
 *
 * This will overwrite the default build_opt.h (committed with -frtti) with
 * RTTI-free configuration.
 *
 * 📊 BINARY SIZE: ~866KB (saves 40KB vs RTTI mode)
 *
 * ⚠️ WARNING:
 * ===========
 * • Disables runtime type safety (dynamic_cast → static_cast)
 * • Wrong type casts cause undefined behavior (not safe nullptr)
 * • Use ONLY after thorough testing with RTTI mode
 * • Recommended only for flash-constrained production (<1MB available)
 *
 * 📖 USAGE (Arduino IDE):
 * =======================
 * 1. cd examples/BasicInterpreter
 * 2. cp build_opt_no_rtti.h.example build_opt.h
 * 3. Open BasicInterpreter.ino in Arduino IDE
 * 4. Compile (IDE applies overridden build_opt.h)
 * 5. Verify binary size: Should be ~866KB
 * 6. Upload to ESP32-S3
 *
 * 🔧 USAGE (arduino-cli):
 * =======================
 * cd examples/BasicInterpreter
 * cp build_opt_no_rtti.h.example build_opt.h
 * arduino-cli compile --fqbn esp32:esp32:esp32s3 .
 *
 * 🔄 TO RESTORE DEFAULT (RTTI Mode):
 * ==================================
 * git checkout examples/BasicInterpreter/build_opt.h
 *
 * This restores the committed default RTTI configuration (-frtti).
 *
 * 📊 SIZE COMPARISON:
 * ===================
 * RTTI mode (default):    ~906KB flash
 * RTTI-free mode (this):  ~866KB flash (-40KB savings)
 *
 * Version: 21.1.0
 */

// Enable RTTI-free mode (size optimization)
-DAST_NO_RTTI

// Disable RTTI compiler feature (~40KB savings)
-fno-rtti
